/*
 * Vite copies public files before it knows the content-hashed JavaScript and CSS
 * filenames. scripts/build-sw.mjs replaces this development placeholder in
 * dist/site/ with the versioned production worker and its complete shell list.
 */
